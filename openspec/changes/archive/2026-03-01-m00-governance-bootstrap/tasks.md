## 1. OpenSpec Change Artifacts

- [x] 1.1 Create `m00-governance-bootstrap` change folder.
- [x] 1.2 Author `proposal.md`, `specs/governance-bootstrap/spec.md`, `design.md`, and `tasks.md`.

## 2. Governance Document Updates

- [x] 2.1 Rewrite `AGENTS.md` with mission, canonical source order, repository map rules, OpenSpec workflow contract, task tracking contract, definition of done, scope guardrails, and tech baseline.
- [x] 2.2 Add repository map section for top-level structure and maintenance cadence.

## 3. Tracking Artifacts

- [x] 3.1 Add `docs/master-task-list.md` with required milestone backlog columns.
- [x] 3.2 Add `docs/current-task.md` with active change card fields and acceptance checks.

## 4. OpenSpec Configuration

- [x] 4.1 Enrich `openspec/config.yaml` with OmniClaw context and artifact-specific rules.
- [x] 4.2 Validate change with `openspec validate --type change m00-governance-bootstrap --strict`.
