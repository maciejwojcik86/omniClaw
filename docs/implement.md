No active implementation change.

Hold state

1. Keep `docs/current-task.md` and `docs/plan.md` aligned with the most recently archived milestone.
2. Do not start new implementation work until the next OpenSpec change is opened.
3. Use the archived Nanobot runtime baseline from `2026-03-06-m07b-nanobot-runtime-migration` as the reference point for future changes.

Current baseline notes

- Canonical AGENT runtime is Nanobot.
- Canonical AGENT directories live under `workspace/agents/<agent_name>/`.
- Runtime launch uses explicit Nanobot `--workspace/--config` inputs.
- The next planned milestone is `m08-context-injector`.
